package controller

import (
	"github.com/gin-gonic/gin"
)

func AddPeople(ctx *gin.Context) {
	// DB := database.GetUserCollection()
	// //使用map获取请求参数
	// var people = model.People{}
	// ctx.Bind(&people)
	// //获取参数
	// name := people.Name
	// email := people.Phone
	// password := people.IDNumber
}
